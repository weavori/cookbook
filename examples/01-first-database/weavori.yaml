# Recipe 01: how the data should look.

generate:
  tables:
    customers:
      columns:
        # Derived values are computed inline during generation.
        email:
          formula: "lower(concat(first_name, '.', last_name, '@example.com'))"
